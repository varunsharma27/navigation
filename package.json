{
  "name": "navigation",
  "version": "1.0.0",
  "kibana": {
    "version": "kibana"
  },
  "scripts": {
    "start": "plugin-helpers start",
    "build": "plugin-helpers build",
    "postinstall": "plugin-helpers postinstall"
  },
  "devDependencies": {
    "@elastic/plugin-helpers": "6.1.1"
  }
}
